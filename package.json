{
  "name": "preact-browser-modules",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "live-server",
    "serve": "serve -s",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "preact": "^10.5.5"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "patch-package": "^6.2.2",
    "postinstall-postinstall": "^2.1.0",
    "serve": "^11.3.2"
  }
}
